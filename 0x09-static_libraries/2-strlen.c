#include "main.h"
#include <string.h>

/**
 *_strlen - print the lenght of a string;
 *
 *@s: parameter
 *
 *Return: the count
 */


int _strlen(char *s)
{
	int len;

	for (len = 0; s[len] != '\0'; len++)
	{
	}
	return (len);
}
