#include "main.h"
#include <stdlib.h>

/**
 *_abs - computes the absolute value of an integer
 *
 *@j:is an integer parameter
 *
 * Return: 0 if computing is success
 */

int _abs(int j)
{
	int jabs;

	jabs = abs(j);

	return (jabs);
}
