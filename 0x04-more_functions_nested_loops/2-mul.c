#include"main.h"
#include <ctype.h>

/**
 *mul - checkes if a character is digit
 *
 * @a: is the parameter
 *
 * @b: is a parameter
 *
 * Return: 1 if successful and 0 if not
 */


int mul(int a, int b)
{
	int i = a * b;

	return (i);
}
